# T1 规格 · 动作模板编辑器 + 占位符保护机制（v25）

> 状态：**已拍板**（2026-08-14 用户答复 A/B/C；wayfinder ticket：T1 规格 · 模板编辑器与占位符保护机制 #365）
> 拍板记录：
> - Q-A：引导句**不用占位符**——它就是普通静态文本，用户可改；真正不可改的只有有定死逻辑的动态部分（时间戳/文件名等系统真值）。
> - Q-B：execute 模板唯一编辑点 = 「开始模板」节；动作模板编辑器 = 其余 6 动作 + 提示行（对抗式审查后修正了「双入口同 state」初案）。
> - Q-C：规格整体采纳。
> 范围：本文件只定「模板编辑器 + 动态部分保护」机制规格；配置页骨架（T2a）、双语字典（T3）不在此展开。
> 前置事实：`settings.plugins.tab` Slot 契约已由 Inspect 确认（list 型，注册字段 id/order/label）；现有模板与配置现状来自 `client.js`（v24）。

## 1. 领域术语（domain model）

| 术语 | 定义 |
|---|---|
| 动作模板（Action Template） | 一次动作注入的完整 prompt 骨架 = 静态文本 + 占位符 |
| 静态文本 | 用户可自由编辑的部分（命令、说明、排版、**含引导句**） |
| 占位符（Placeholder） | `{name}` 形式，注入时由系统替换为真值；用户**只能移动位置，不能改真值** |
| 强制占位符（Required Placeholder） | 某模板保存时必须包含的占位符；缺失 → 拒绝保存 |
| 未知占位符（Unknown Placeholder） | 模板中出现但未在占位符全集内的 `{xxx}`；保存时拒绝并提示 |
| 转义（Escape） | `{{name}}` → 渲染为字面量 `{name}`（不替换） |
| 真值预览（Preview） | 编辑时把占位符替换为示例真值的只读渲染区 |

## 2. 占位符全集（5 个）

| 占位符 | 语义 | 注入来源 | 使用模板 |
|---|---|---|---|
| `{url}` | issue/map 完整 URL `https://github.com/{owner}/{repo}/issues/{n}` | host 快照 repo + issue number | 诊断/修复/讨论/执行 |
| `{number}` | issue 号 | 快照 | 执行（兼容旧 custom） |
| `{title}` | issue 标题 | 快照 | 执行（兼容旧 custom） |
| `{ts}` | 交接时间戳 `YYYYMMDD-HHMMSS` | 第一击注入时生成并记忆（handoffTs） | 交接第一击 |
| `{path}` | 交接文档**绝对路径**（如 `D:/repo/.scratch/handoff/20260814-172113-修复提示词.md`） | `cwd/.scratch/handoff/{file}` 拼接；真实文件名由探测按 `{handoffTs}-*.md` 前缀发现 | 交接第二击 |
| `{file}` | 交接文档**文件名**（如 `20260814-172113-修复提示词.md`） | 前缀探测发现的真实文件名（含 AI 短标题） | 交接第二击 |

> **引导句不是占位符**（Q-A 拍板）：「从第一性原理出发完成任务，并对抗式审查。」是默认模板静态文本的一部分，用户可自由编辑/删除——它没有定死逻辑。
> ⚠️ **对抗式审查发现 1（规格关键修正）**：v24 实现里第二击读 `handoffTs + '.md'`。若用户把第一击模板文件名结构改成如 `交接-{ts}.md`，第二击会读错文件（读 `{ts}.md`）。**修正**：`{file}` 真值 = 第一击模板渲染后解析出的实际文件名（提取第一击注入文本中 `.scratch/handoff/<name>.md` 的 `<name>`，含 {ts} 替换值），第二击 `{file}` = `<name>.md`。这才是「模板写什么名就读什么名」的完整实现；解析失败兜底 `handoffTs + '.md'`。

## 3. 七个动作模板（默认文本 = 现状代码文本，空 = 用默认）

| 动作 id | 触发 | 默认模板文本 | 强制占位符 |
|---|---|---|---|
| `diagnose` 诊断 | needs-triage 按钮 | `/triage\n{url}\n\n从第一性原理出发完成任务，并对抗式审查。` | `{url}` |
| `fix` 修复 | bug 按钮 | `/wayfinder\n{url}\n\n从第一性原理出发完成任务，并对抗式审查。` | `{url}` |
| `discuss` 讨论 | wayfinder:grilling 按钮 | `/wayfinder\n{url}\n\n从第一性原理出发完成任务，并对抗式审查。` | `{url}` |
| `execute` 执行 | 默认动作按钮 | `（前缀开关注入 /wayfinder\n）+ {url}\n\n从第一性原理出发完成任务，并对抗式审查。`（custom 为空时） | `{url}` |
| `handoff1` 交接第一击 | 交接段第一击 | `/handoff 把当前会话生成交接文档，写到 .scratch/handoff/{ts}-<短标题>.md（相对当前工作目录）。<短标题> 是你给这次交接起的一个简短标题（中文 ≤10 字 / 英文 ≤20 字符，跟随当前会话语言，用连字符或下划线代替空格），让人一眼认出这是哪件事的交接。\n\n交接文档是给一个没有本次会话记忆的 agent 接手的——请站在它的视角，确保它能凭文档无缝继续，而不是靠猜或回翻本次会话。从第一性原理出发。` | `{ts}` |
| `handoff2` 交接第二击 | 交接段第二击 | `请阅读 {path}（上一会话生成的交接文档），复述你的理解后再继续推进：\n\n## 复述理解\n- [ ] 结论：本会话已确认的决定与成果\n- [ ] 未完成事项：下一步要继续的事\n- [ ] 建议 skill：新会话接手时应加载的技能\n- [ ] 把以上三点复述给我；若有遗漏或不确定 → 先问我确认，不猜\n\n## 继续推进\n- [ ] 从第一性原理出发，继续完成未完成事项` | `{path}` |
| `fixate` 沉淀 | 沉淀按钮 | 现状 FIXATE_PROMPT 全文（「里程碑固化点…」） | 无 |

**执行模板与前缀开关的关系**（关键决策）：
- `execute` 模板保存的是「body 部分」（custom 语义），`withWayfinder` 前缀开关作为**独立系统段**在注入时前置（`开关 ? '/wayfinder\n' : ''` + 模板渲染结果）。
- 旧 custom 模板含 {number} {url} {title} 占位符 → 迁移后全部仍可用（全集已含）。
- 恢复默认 = 清空为 `''`（注入时走内置默认文本）。

## 4. 保存校验规则（顺序执行）

1. **转义预处理**：`{{name}}` 先替换为哨兵 token（防误报），校验结束后还原。
2. **未知占位符检测**：`/\{([a-z][a-z0-9]*)\}/gi` 提取，减去全集（{url}{number}{title}{ts}{file}）→ 非空则**拒绝保存**，提示「未知占位符 {xxx}」（中英双语，跟随 locale）。
3. **强制占位符检测**：逐模板校验其强制集 ⊆ 提取集合 → 缺失则**拒绝保存**，提示「模板「X」缺少强制占位符 {url}」（中英双语，列出全部缺失项）。
4. 通过 → 写入 `dsws.templates`，toast「已保存」（面板内 toast，静默成功只反馈一次）。
5. 错误反馈只在面板 toast（用户偏好：反馈只在面板 toast）。

## 5. 编辑交互（方案 B：textarea + 占位符面板 + 预览）

- 每个动作一行卡片：名称 + 编辑 textarea（等宽字体，复用 `dsws-ta` 样式）。
- 卡片下方「可用占位符」chips 行：点击 chip 在光标处插入 `{name}` 文本；强制占位符带「必」标记。
- 卡片内「真值预览」只读区（可选折叠）：实时渲染替换效果，示例真值 = 当前快照首个 issue 的 URL/号/标题 + 当前时间戳 + 示例文件名。
- **不做 contenteditable chips**（React 中光标管理易碎、无 bundler 风险高）；「只读」的实质安全由保存校验保证（删/改强制占位符即拒绝）。
- 每卡「恢复默认」按钮（清空 → `''`）；页面级「恢复全部默认」。
- ⚠️ **对抗式审查发现 2**：textarea 无法局部高亮占位符；overlay 高亮方案在中文/换行对齐上有坑。**结论：不做编辑器内高亮**，占位符认知靠 chips 行 + 预览区承担。

## 6. 配置页结构（T2a 骨架规格）

挂载 `settings.plugins.tab`（id: `dsws-settings`，label: `Waystation`，与 opencode 主题同款）。分节：

1. **面板**：默认高度三档 1/4 · 1/2 · 2/3（默认 1/2，落地 `size.h = screenH * ratio`）。
2. **开始模板**：`/wayfinder` 前缀开关 + **execute 模板编辑器（唯一编辑点）** + 恢复默认。（Q-B 拍板：一个配置项只在一处编辑）
3. **外观**：图标方案（罗盘/灯塔/雷达/图钉）+ 沉淀动作词（沉淀/落纸/存档/快照）。
4. **动作模板编辑器**：六个动作卡片（诊断/修复/讨论/交接第一击/交接第二击/沉淀）+ 顶部提示行「执行动作的模板请在『开始模板』节编辑」→（锚点跳转）。

- ⚠️ **对抗式审查发现 3（隐性 bug）**：现状 `ui`（图标/动作词）与面板尺寸/位置存在**内存 store，不持久化**（只有 startCfg 进 localStorage）。配置页落地时：外观/面板高度/模板全部持久化到 localStorage；**保存后广播同步所有会话 store**（遍历 `stores` + `shared` 的 `ui`/`size`/模板引用），否则多会话下配置不生效或漂移。
- 入口收敛：面板内 StartCfgModal **移除**；RunPanel 保留「已加载」状态展示 + 加「打开配置」按钮直达设置页。侧栏入口（sidebar.footer.action）去留 = 用户未决项，不在本规格内。

## 7. 存储模型与迁移

```js
// localStorage
dsws.templates = { diagnose:'', fix:'', discuss:'', execute:'', handoff1:'', handoff2:'', fixate:'' }  // 空=内置默认
dsws.cfg = { withWayfinder: true, panelHeight: 'half', icon: 'compass', word: '沉淀' }
```

- 首次加载检测旧 `dsws.startCfg`：`custom` 非空 → 迁入 `templates.execute`；`withWayfinder` → 迁入 `cfg.withWayfinder`；迁移成功后 `localStorage.removeItem('dsws.startCfg')`。
- 注入函数统一走 `renderTemplate(tpl, values)`：转义还原 → 替换已知占位符 → 未知占位符保留原样（保存层已拦截，运行层兜底不炸）。

## 8. 注入时机与两击一致性（双保险）

- 第一击：`{ts}` 注入时生成 `handoffTs` 并记忆（沿用 v24）；同时解析记录实际文件名 → 供第二击 `{file}`。
- 第二击：优先记忆的 `{file}`；未点过第一击（如刷新后）→ host `wf.handoffLatest` 兜底（沿用 v24）。
- 模板层强制 `{ts}`/`{file}` + 系统记忆双保险 = 用户自定义无法破坏流程（改文件名结构也一致，见发现 1）。

## 9. 错误文案（中英双语 key 结构，T3 填字典）

- `tpl.unknownPlaceholder`：`未知占位符 {x}` / `Unknown placeholder {x}`
- `tpl.missingRequired`：`模板「{tpl}」缺少强制占位符：{list}` / `Template "{tpl}" is missing required placeholder(s): {list}`
- `tpl.saved`：`已保存` / `Saved`
- 校验提示遵循用户偏好：**全界面禁 emoji，SVG 图标；反馈只在面板 toast**。

## 10. 验收清单（T2b 完成后可测）

- [ ] 设置 → 插件 → Waystation 出现配置页，四节齐全
- [ ] 每个动作模板可改静态文本（含引导句）；占位符删除/改写强制项 → 保存被拒并提示缺哪个（中英双语）
- [ ] 未知占位符（手滑 {urll}）→ 保存被拒并提示
- [ ] `{{url}}` 转义后注入为字面量 `{url}`，不触发替换/校验
- [ ] 交接第一击改文件名结构（如 交接-{ts}.md）→ 第二击仍读同一文件（发现 1 修复验证）
- [ ] 执行按钮注入 = 前缀开关 + 模板渲染结果；恢复默认后回到内置文本
- [ ] 旧 startCfg（custom + 开关）首次升级后自动迁移，旧 key 清除
- [ ] 多会话：改配置后所有会话面板/按钮即时生效
- [ ] 面板高度三档可切换且重启后保持
- [ ] 外观（图标/动作词）选择后重启保持
