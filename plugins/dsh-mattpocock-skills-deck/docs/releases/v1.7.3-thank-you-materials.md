# 四案票致谢与版本注记材料（v1.7.3）—— 供 #285 批量关闭使用

> 本材料由 #286（发版 v1.7.3 与工单收尾）准备，实际关闭动作由姊妹票 #285（发版后 · 四案票致谢关闭与 PR 收尾）执行。
> 发布凭据：Git tag v1.7.3（commit 1967dcd）｜GitHub Release https://github.com/FeatherHunter/dsh-mattpocock-skills-deck/releases/tag/v1.7.3 ｜构建 DSW_VERSION=v1.7.3 ｜npm 包 dsh-mattpocock-skills-deck@1.7.3（63 文件，shasum 3b523bf...，公开仓免费）｜验证：npm run verify 核心通过、test:smoke 5/5、三矩阵 216/216 + 552/552 + 47/47。

---

## #234 致谢关闭稿（健康检查误报“未检测到核心技能套件”）

感谢报告！

本问题已随 **v1.7.3** 修复并发布。

- **根因收口**：探测器不再自带平行尺子 —— 判装只问 DSH 注册表（B 语义：目录里有名字即绿，含异处副本绿+来源路径提示），红牌区分“缺失/名片无效”，等待态有界且事件驱动；旧轨（九格视图）与三套平行探测口径已退役，统一路径入口保证同目录三种写法快照一致。
- **你的场景**：`~/.agents/skills` 下 10 项核心技能（wayfinder 等）已完整安装且 SKILL.md 合法时，面板健康检查现已如实报“核心技能套件已安装”，横幅不再误报。
- **验证建议**：升级到 **dsh-mattpocock-skills-deck@1.7.3** 后完全退出 DSH 再重开（Ctrl+F5 刷新），在 Windows/macOS/Linux 均应为绿；若仍复现，请附“环境检查”页技能行原文与 DSW_VERSION（右下角）另行反馈。
- **关联**：规格 #276、地图 #278、交付 #279/#280/#281/#284。

感谢你帮我们把“提示与事实不一致”的最后一处缝合！

---

## #262 致谢关闭稿（macOS 技能检测误报“无法探测用户主目录”）

感谢报案与精准定位！

**v1.7.3** 已发布，本问题已修复。

- **修复点**：平台层 getHome 已统一为 `os.homedir()` 单一真源，darwin/linux 的 `process.env.HOME` 仅作主源为空时的例外兜底；路径拼接全量走 `path.join`，不再硬编码分隔符；探测整链增加事件驱动的等待与重试，启动早期 `os.homedir()` 为空的瞬间不会再静默跳过整段探测。
- **你的场景**：macOS 上即使主进程启动早期 `.agents/skills` 探测曾被跳过，现已能在技能目录就绪后有界重试并转为绿灯；原 `cmd.exe` 分支与反斜杠硬编码两类缺陷已在 #280 平台层迁移中消除。
- **验证建议**：升级到 **1.7.3** 后在 macOS 重启 DSH，健康检查技能行应为绿；若仍见“无法探测用户主目录”，请带环境检查该行 detail 原文与版本回报，我们另立平台层缺陷单深查。

再次感谢！

---

## #271 致谢关闭稿（核心技能套件误报未安装 + 一键安装提示词未闭环）

感谢详尽的复现与诊断（尤其 2026-08-28 的现场诊断）！

本双向失效已随 **v1.7.3** 整体闭环并发布。

- **探测一致**：健康检查与真实加载路径已收敛到同一份扫描结果 —— 注册表查询为判装唯一尺，辅助轻探仅用于红牌解释；控制台/日志已可输出探测依据（目录、文件、为何判缺失）。
- **安装闭环**：注入提示词已升级为六步作业指导书（平台中立、逐个按三条标准核对、只装缺失、覆盖半坏、复检至全绿、诚实汇报），且安装目标恒为 DSH 读取的 `~/.agents/skills`，不受沙箱写限制而失败的替代说明与验证步骤已补齐。
- **验证建议**：升级到 **1.7.3** 后，干净安装+重启应报“核心技能套件已安装（10 个）”，点击“帮我安装Matt技能套件”后 AI 按模板可一步到位并在刷新后变绿；请在 Windows 11 与 macOS 下各复测一次。

由衷感谢你把“说的话和电脑里的事实不一致”的全链证据链补齐！

---

## #274 致谢关闭稿（状态栏横幅持续误报，即使 11 个技能目录全部就位）

感谢探针级定位与五类根因拆解！

本横幅顽固误报已随 **v1.7.3** 修复并发布（你的 PR #273 的 1~5 类修复均已被吸收进主线对应交付）。

- **对应关系**：
  - 根因 1（os.homedir 早期空串）→ #280/#281 平台层与等待规则已覆盖；
  - 根因 2（cwd 相对探测缺失）→ 已由统一路径入口与链快照语义在结构上消除；
  - 根因 3（c8 错位）与 根因 4/5（grill-with-docs 等不在清单/渲染数组）→ #282 名单单一真源与 #284 旧轨退役已整体收口（25 项以 https://github.com/mattpocock/skills/tree/main/skills/engineering 与 https://github.com/mattpocock/skills/tree/main/skills/productivity 为准）；
  - 根因 6（statusDerive 拼写错误 `setup-mattpocock-skills`）→ 该产物文件已随旧视图整体退役（#284 删除 wf.status/statusDerive），链上不再有该错拼分支。
- **你的场景**：`~/.agents/skills` 下 11 个目标技能目录全部存在（含 setup-matt-pocock-skills 与 grill-with-docs）时，状态栏横幅现已不再报警；面板技能列表（grill-me / grill-with-docs）渲染真源已补齐。
- **验证建议**：升级到 **1.7.3** 后重启 DSH，横幅应消失，技能列表应出现此前缺失的两项；若仍见横幅，请附 wf.chain 快照中 ``skill:setup-matt-pocock-skills`` 行的 detail 原文。

特别感谢你用“直接 import 安装副本 + stub ctx 跑 wf.status”抓到的真实响应！

---

## PR #273 关闭稿（fix(host): probeSkill 双源兜底 + grill-with-docs 第 11 名）

感谢深入的四类根因分析与 draft 实现！

你的分析已被 **v1.7.3** 整体实现吸收并替代：

- darwin/linux getHome 的 envFallback 例外条款 → 已在 #280 平台层迁移中以“主源单一真相 + 空时兜底”的更严格契约落地；
- cwd 相对探测源 → 已由 #279 统一路径入口与链快照的“原始描述进、规整键出”在结构上消除串味，无需额外兜底分支；
- SKILL_PROBE_NAMES 11 名与 c8 索引修正 → 已在 #282 名单单一真源（25 项）与构建期生成中统一，索引类魔法数已退役；
- client SKILLS 19→21 与 TYPE_SKILLS.grilling 补 grill-with-docs → 已在 #282 与 #284 旧轨退役中以契约层真源同步至渲染层；
- statusDerive 拼写错误 → 该产物已随 #284 旧视图退役整体移除。

因此本 PR 不再单独合并，以 v1.7.3 的六步交付为最终形态关闭。再次感谢你的探针与回归用例（25 项断言）为诊断提供了关键证据！

---

> 批量关闭执行人（#285）请逐一复制上方对应稿件为评论，注明版本 v1.7.3 与发布链接 https://github.com/FeatherHunter/dsh-mattpocock-skills-deck/releases/tag/v1.7.3 后关闭；PR #273 请按最后一节关闭。
